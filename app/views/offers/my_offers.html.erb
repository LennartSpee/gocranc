  <div class="container m-5">
    <h1>Adventures you want to share</h1>
  </div>
  <div style="width: 100%; height: 400px;" data-controller="mapbox"
  data-mapbox-markers-value="<%= @markers.to_json %>" data-mapbox-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
<%# <div class="mt-5 mx-5 cards content"> %>
<%# @offers.each do |offer| %>
    <%#= offer.title %>
    <%#= link_to 'Show', offer_path(offer), class: "btn btn-accent mt-2 w-50 p-3" %>
<%# end %>
<%# </div> %>

<div class="mt-5 mx-5 cards content">
  <% @offers.each do |offer| %>
     <%= link_to offer_path(offer) do %>
  <%#= link_to 'Show', offer_path(offer.id) do%>
    <div class="card-category" style="background-image: url('<%= offer.photo.key.present? ? cl_image_path(offer.photo.key) :  "https://images.unsplash.com/photo-1477466535227-9f581b3eec21?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1931&q=80" %>')">
      <%= offer.title %><%#= link_to 'Show', offer_path(offer.id) %>
    </div>
  <% end %>
<% end %>
</div>
